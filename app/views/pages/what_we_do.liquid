---
title: "Agence"
published: true
slug: agence
---

{% extends index %}

{% block 'header' %}

  <div class="header">
    <div class="jumbotron" id="section-why-choose">
      <div class="container">
        {% assign url = 'applidget-logo/blue.png' %}
        {% include 'topmenu' %}
      </div>

      {% include 'what_we_do' %}
    </div>
  </div>

{% endblock %}

{% block 'main' %}

  <div class="jumbotron" id="main-technologies">
    <div class="container page">

      {% with_scope published: true %}
        {% for object in contents.technologies %}

          {% assign icon = false %}
          {% assign link_url = false %}
          {% include 'page_content' %}

        {% endfor %}
      {% endwith_scope %}

    </div>
  </div>

{% endblock %}